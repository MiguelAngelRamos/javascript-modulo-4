let producto = 'Notebook Gamer MSI pantalla 17 pulgadas';
let password = 'sectr'
// Conocer la longitud de un string

console.log(producto.length);

//* validacion que la contraseña debe ser superior a 8 caracteres

// if(password.length >= 8) {
//   alert('Tu password cumple con minimo de caracteres');
// } else {
//   alert('Tu password no cumple con minimo de caracteres');
// }

//* Concatenar utilizamos el +

let nombre = "Sofia";
let rol = "ADMIN";
let profesion = "Ing. Software";

// let mensaje = nombre + ", tiene el rol de: " + rol + " y su profesion es: " + profesion;

let mensaje = `${nombre}, tiene el rol de ${rol} y su profesion es: ${profesion}`;
// alt (presionando) + 96 (dejar de presionar el alt)

console.log(mensaje);