/* Vamos a calcular el area de un cuadrado */

let lado = prompt("Ingrese el lado del cuadrado") // INPUT - ENTRADA

// let area = lado * lado; // PROCESO
let area = Math.pow(lado, 2);

alert("El area del cuadrado es " + area); // OUTPUT - SALIDA

